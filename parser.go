package gooml

import (
	"slices"
)

/* func parseOmlsSelfLoading(input string) (output string, err error) {
	parsableOmls := LoadOmls()
	parseOmls(parsableOmls, input)
   }

   func parseOmls(omls []*oml, input string) (output string, err error) {
		parseOmlTracker := newOMLTracker()	
		defer omlTracker.release()
			loop over iput string characters
				if character is < and next character is not < as well 
					make a string of the characters up to the next >
					if the string is not empty check if it is in the list of omls
					if it is in the list of omls check the omlTracker
					if the oml is not tracked then add it, if it is tracked then remove it
	}
*/

